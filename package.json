{
  "name": "lod-vault",
  "version": "2.0.0",
  "private": true,
  "description": "Tests and development tooling for the LODVault browser extension.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "build:background": "node scripts/build-background.mjs",
    "build": "node scripts/build.mjs",
    "package:release": "npm run build"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
